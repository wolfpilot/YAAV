// Config
const _defaults = {

};

class Pulse {
  constructor(x, y, startRadius, endRadius, duration, stroke) {
    console.log('PULSE!');

    // const
  }

  // as initialised with or defaults
  state = {
    x: window.innerWidth / 2,
    y: window.innerHeight / 2,
    radius: 100,
    duration: 2000,
    stroke: 'blue'
  }
}

export default Pulse;
